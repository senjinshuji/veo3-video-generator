services:
  - type: web
    name: veo3-video-generator
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: FAL_KEY
        sync: false